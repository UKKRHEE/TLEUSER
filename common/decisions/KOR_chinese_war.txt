
KOR_first_plan_lightning = {
	KOR_zhili_attack_1 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = zhili_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
		}
		visible = { tag = KOR has_war_with = ZLC has_global_flag = global_CHI_defence_the_korea } 
		highlight_states = {
			highlight_state_targets = { 
				state = 1012
				state = 609
				state = 608
				state = 1097
				state = 1033
				state = 614
				state = 1101
			} 
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = zhili_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = zhili_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}
			set_country_flag = flag_KOR_zhili_attack_2
		}
	}
	KOR_zhili_attack_2 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = zhili_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_zhili_attack_2
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_zhili_attack_2
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 1012
				state = 609
				state = 608
				state = 1097
				state = 1033
				state = 614
				state = 1101
			} 
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = zhili_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = zhili_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
			set_country_flag = flag_KOR_zhili_attack_3
		}
	}
	KOR_zhili_attack_3 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = zhili_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_zhili_attack_3
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_zhili_attack_3
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 1012
				state = 609
				state = 608
				state = 1097
				state = 1033
				state = 614
				state = 1101
			} 
		}
		
		fire_only_once = yes
		days_remove = 10
		complete_effect = { if = { limit = { is_ai =no NOT = { has_country_flag = flag_KOR_zhili_attack_4 } } army_experience = -20 add_command_power = -100 } country_event = { id = korea.352 }
			every_state = {
				limit = {
					has_state_flag = zhili_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
		}	
		remove_effect = {
			set_country_flag = flag_KOR_zhili_attack_4
		}
	}
	KOR_shandong_attack_1 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = shandong_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
		}
		visible = { tag = KOR has_war_with = ZLC has_global_flag = global_CHI_defence_the_korea } 
		highlight_states = {
			highlight_state_targets = { 
				state = 1100
				state = 1050
				state = 597
				state = 1049
				state = 1035
				state = 1004
				state = 743
				state = 1034
			}  
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = shandong_province_state
					NOT = {
						state = 743
						state = 1034
					}
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = shandong_province_state
					NOT = {
						state = 743
						state = 1034
					}
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
			set_country_flag = flag_KOR_shandong_attack_2
		}
	}
	KOR_shandong_attack_2 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = shandong_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_shandong_attack_2
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_shandong_attack_2
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 1100
				state = 1050
				state = 597
				state = 1049
				state = 1035
				state = 1004
				state = 743
				state = 1034
			}  
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = shandong_province_state
					NOT = {
						state = 743
						state = 1034
					}
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = shandong_province_state
					NOT = {
						state = 743
						state = 1034
					}
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
			set_country_flag = flag_KOR_shandong_attack_3
		}
	}
	KOR_shandong_attack_3 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = shandong_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_shandong_attack_3
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_shandong_attack_3
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 1100
				state = 1050
				state = 597
				state = 1049
				state = 1035
				state = 1004
				state = 743
				state = 1034
			}  
		}
		
		fire_only_once = yes
		days_remove = 10
		complete_effect = { if = { limit = { is_ai =no NOT = { has_country_flag = flag_KOR_shandong_attack_4 } } army_experience = -20 add_command_power = -100 } country_event = { id = korea.352 }
			every_state = {
				limit = {
					has_state_flag = shandong_province_state
					NOT = {
						state = 743
						state = 1034
					}
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
		}	
		remove_effect = {
			set_country_flag = flag_KOR_shandong_attack_4
		}
	}
	KOR_shanxi_attack_1 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = shanxi_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
		}
		visible = { tag = KOR has_war_with = ZLC has_global_flag = global_CHI_defence_the_korea } 
		highlight_states = {
			highlight_state_targets = { 
				state = 1099
				state = 615
				state = 1052
			}  
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = shanxi_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = shanxi_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
			set_country_flag = flag_KOR_shanxi_attack_2
		}
	}
	KOR_shanxi_attack_2 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = shanxi_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_shanxi_attack_2
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_shanxi_attack_2
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 1099
				state = 615
				state = 1052
			}  
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = shanxi_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = shanxi_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
			set_country_flag = flag_KOR_shanxi_attack_3
		}
	}
	KOR_shanxi_attack_3 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = shanxi_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_shanxi_attack_3
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_shanxi_attack_3
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 1099
				state = 615
				state = 1052
			}  
		}
		
		fire_only_once = yes
		days_remove = 10
		complete_effect = { if = { limit = { is_ai =no NOT = { has_country_flag = flag_KOR_shanxi_attack_4 } } army_experience = -20 add_command_power = -100 } country_event = { id = korea.352 }
			every_state = {
				limit = {
					has_state_flag = shanxi_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
		}	
		remove_effect = {
			set_country_flag = flag_KOR_shanxi_attack_4
		}
	}
	KOR_shaanxi_attack_1 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = shaanxi_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
		}
		visible = { tag = KOR has_war_with = ZLC has_global_flag = global_CHI_defence_the_korea } 
		highlight_states = {
			highlight_state_targets = { 
				state = 622
				state = 744
			}  
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = shaanxi_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = shaanxi_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
			set_country_flag = flag_KOR_shaanxi_attack_2
		}
	}
	KOR_shaanxi_attack_2 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = shaanxi_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_shaanxi_attack_2
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_shaanxi_attack_2
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 622
				state = 744
			}  
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = shaanxi_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = shaanxi_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
			set_country_flag = flag_KOR_shaanxi_attack_3
		}
	}
	KOR_shaanxi_attack_3 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = shaanxi_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_shaanxi_attack_3
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_shaanxi_attack_3
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 622
				state = 744
			}  
		}
		
		fire_only_once = yes
		days_remove = 10
		complete_effect = { if = { limit = { is_ai =no NOT = { has_country_flag = flag_KOR_shaanxi_attack_4 } } army_experience = -20 add_command_power = -100 } country_event = { id = korea.352 }
			every_state = {
				limit = {
					has_state_flag = shaanxi_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
		}	
		remove_effect = {
			set_country_flag = flag_KOR_shaanxi_attack_4
		}
	}
	KOR_henan_attack_1 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = henan_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
		}
		visible = { tag = KOR has_war_with = ZLC has_global_flag = global_CHI_defence_the_korea } 
		highlight_states = {
			highlight_state_targets = { 
				state = 607
				state = 1057
				state = 1008
				state = 1074
			}  
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = henan_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = henan_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
			set_country_flag = flag_KOR_henan_attack_2
		}
	}
	KOR_henan_attack_2 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = henan_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_henan_attack_2
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_henan_attack_2
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 607
				state = 1057
				state = 1008
				state = 1074
			}
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = henan_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = henan_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
			set_country_flag = flag_KOR_henan_attack_3
		}
	}
	KOR_henan_attack_3 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = henan_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_henan_attack_3
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_henan_attack_3
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 607
				state = 1057
				state = 1008
				state = 1074
			}
		}
		
		fire_only_once = yes
		days_remove = 10
		complete_effect = { if = { limit = { is_ai =no NOT = { has_country_flag = flag_KOR_henan_attack_4 } } army_experience = -20 add_command_power = -100 } country_event = { id = korea.352 }
			every_state = {
				limit = {
					has_state_flag = henan_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
		}	
		remove_effect = {
			set_country_flag = flag_KOR_henan_attack_4
		}
	}
	KOR_anhui_attack_1 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = anhui_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
		}
		visible = { tag = KOR has_war_with = ZLC has_global_flag = global_CHI_defence_the_korea } 
		highlight_states = {
			highlight_state_targets = { 
				state = 606
				state = 749
			}
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = anhui_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = anhui_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
			set_country_flag = flag_KOR_anhui_attack_2
		}
	}
	KOR_anhui_attack_2 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = anhui_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_anhui_attack_2
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_anhui_attack_2
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 606
				state = 749
			}
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = anhui_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = anhui_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
			set_country_flag = flag_KOR_anhui_attack_3
		}
	}
	KOR_anhui_attack_3 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = anhui_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_anhui_attack_3
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_anhui_attack_3
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 606
				state = 749
			}
		}
		
		fire_only_once = yes
		days_remove = 10
		complete_effect = { if = { limit = { is_ai =no NOT = { has_country_flag = flag_KOR_anhui_attack_4 } } army_experience = -20 add_command_power = -100 } country_event = { id = korea.352 }
			every_state = {
				limit = {
					has_state_flag = anhui_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
		}
		remove_effect = {
			set_country_flag = flag_KOR_anhui_attack_4
		}
	}
	KOR_jiangsu_attack_1 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = jiangsu_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
		}
		visible = { tag = KOR has_war_with = ZLC has_global_flag = global_CHI_defence_the_korea } 
		highlight_states = {
			highlight_state_targets = { 
				state = 598
				state = 1051
				state = 1040
				state = 997
				state = 613
				state = 1038
				state = 1036
				state = 1037
			}
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = jiangsu_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = jiangsu_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
			set_country_flag = flag_KOR_jiangsu_attack_2
		}
	}
	KOR_jiangsu_attack_2 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = jiangsu_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_jiangsu_attack_2
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_jiangsu_attack_2
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 598
				state = 1051
				state = 1040
				state = 997
				state = 613
				state = 1038
				state = 1036
				state = 1037
			}
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = jiangsu_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = jiangsu_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
			set_country_flag = flag_KOR_jiangsu_attack_3
		}
	}
	KOR_jiangsu_attack_3 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = jiangsu_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_jiangsu_attack_3
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_jiangsu_attack_3
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 598
				state = 1051
				state = 1040
				state = 997
				state = 613
				state = 1038
				state = 1036
				state = 1037
			}
		}
		
		fire_only_once = yes
		days_remove = 10
		complete_effect = { if = { limit = { is_ai =no NOT = { has_country_flag = flag_KOR_jiangsu_attack_4 } } army_experience = -20 add_command_power = -100 } country_event = { id = korea.352 }
			every_state = {
				limit = {
					has_state_flag = jiangsu_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
		}	
		remove_effect = {
			set_country_flag = flag_KOR_jiangsu_attack_4
		}
	}
	KOR_zhejiang_attack_1 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = zhejiang_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
		}
		visible = { tag = KOR has_war_with = ZLC has_global_flag = global_CHI_defence_the_korea } 
		highlight_states = {
			highlight_state_targets = { 
				state = 596
				state = 1042
				state = 1041
			}
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = zhejiang_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = zhejiang_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
			set_country_flag = flag_KOR_zhejiang_attack_2
		}
	}
	KOR_zhejiang_attack_2 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = zhejiang_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_zhejiang_attack_2
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_zhejiang_attack_2
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 596
				state = 1042
				state = 1041
			} 
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = zhejiang_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = zhejiang_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
			set_country_flag = flag_KOR_zhejiang_attack_3
		}
	}
	KOR_zhejiang_attack_3 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = zhejiang_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_zhejiang_attack_3
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_zhejiang_attack_3
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 596
				state = 1042
				state = 1041
			} 
		}
		
		fire_only_once = yes
		days_remove = 10
		complete_effect = { if = { limit = { is_ai =no NOT = { has_country_flag = flag_KOR_zhejiang_attack_4 } } army_experience = -20 add_command_power = -100 } country_event = { id = korea.352 }
			every_state = {
				limit = {
					has_state_flag = zhejiang_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
		}	
		remove_effect = {
			set_country_flag = flag_KOR_zhejiang_attack_4
		}
	}
	KOR_fujian_attack_1 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = fujian_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
		}
		visible = { tag = KOR has_war_with = ZLC has_global_flag = global_CHI_defence_the_korea } 
		highlight_states = {
			highlight_state_targets = { 
				state = 595
				state = 1043
				state = 1055
				state = 1044
			}
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = fujian_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = fujian_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
			set_country_flag = flag_KOR_fujian_attack_2
		}
	}
	KOR_fujian_attack_2 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = fujian_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_fujian_attack_2
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_fujian_attack_2
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 595
				state = 1043
				state = 1055
				state = 1044
			} 
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = fujian_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = fujian_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
			set_country_flag = flag_KOR_fujian_attack_3
		}
	}
	KOR_fujian_attack_3 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = fujian_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_fujian_attack_3
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_fujian_attack_3
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 595
				state = 1043
				state = 1055
				state = 1044
			}
		}
		
		fire_only_once = yes
		days_remove = 10
		complete_effect = { if = { limit = { is_ai =no NOT = { has_country_flag = flag_KOR_fujian_attack_4 } } army_experience = -20 add_command_power = -100 } country_event = { id = korea.352 }
			every_state = {
				limit = {
					has_state_flag = fujian_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
		}	
		remove_effect = {
			set_country_flag = flag_KOR_fujian_attack_4
		}
	}
	KOR_jiangxi_attack_1 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = jiangxi_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
		}
		visible = { tag = KOR has_war_with = ZLC has_global_flag = global_CHI_defence_the_korea } 
		highlight_states = {
			highlight_state_targets = { 
				state = 1062
				state = 600
				state = 1061
			}
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = jiangxi_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = jiangxi_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
			set_country_flag = flag_KOR_jiangxi_attack_2
		}
	}
	KOR_jiangxi_attack_2 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = jiangxi_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_jiangxi_attack_2
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_jiangxi_attack_2
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 1062
				state = 600
				state = 1061
			}
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = jiangxi_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = jiangxi_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
			set_country_flag = flag_KOR_jiangxi_attack_3
		}
	}
	KOR_jiangxi_attack_3 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = jiangxi_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_jiangxi_attack_3
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_jiangxi_attack_3
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 1062
				state = 600
				state = 1061
			}
		}
		
		fire_only_once = yes
		days_remove = 10
		complete_effect = { if = { limit = { is_ai =no NOT = { has_country_flag = flag_KOR_jiangxi_attack_4 } } army_experience = -20 add_command_power = -100 } country_event = { id = korea.352 }
			every_state = {
				limit = {
					has_state_flag = jiangxi_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
		}	
		remove_effect = {	
			set_country_flag = flag_KOR_jiangxi_attack_4
		}
	}
	KOR_hubei_attack_1 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = hubei_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
		}
		visible = { tag = KOR has_war_with = ZLC has_global_flag = global_CHI_defence_the_korea } 
		highlight_states = {
			highlight_state_targets = { 
				state = 1009
				state = 1048
				state = 620
				state = 1056
				state = 750
			}
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = hubei_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = hubei_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
			set_country_flag = flag_KOR_hubei_attack_2
		}
	}
	KOR_hubei_attack_2 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = hubei_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_hubei_attack_2
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_hubei_attack_2
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 1009
				state = 1048
				state = 620
				state = 1056
				state = 750
			}
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = hubei_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = hubei_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
			set_country_flag = flag_KOR_hubei_attack_3
		}
	}
	KOR_hubei_attack_3 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = hubei_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_hubei_attack_3
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_hubei_attack_3
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 1009
				state = 1048
				state = 620
				state = 1056
				state = 750
			}
		}
		
		fire_only_once = yes
		days_remove = 10
		complete_effect = { if = { limit = { is_ai =no NOT = { has_country_flag = flag_KOR_hubei_attack_4 } } army_experience = -20 add_command_power = -100 } country_event = { id = korea.352 }
			every_state = {
				limit = {
					has_state_flag = hubei_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
		}	
		remove_effect = {
			set_country_flag = flag_KOR_hubei_attack_4
		}
	}
	KOR_hunan_attack_1 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = hunan_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
		}
		visible = { tag = KOR has_war_with = ZLC has_global_flag = global_CHI_defence_the_korea } 
		highlight_states = {
			highlight_state_targets = { 
				state = 602
				state = 1054
			}
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = hunan_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = hunan_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
			set_country_flag = flag_KOR_hunan_attack_2
		}
	}
	KOR_hunan_attack_2 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = hunan_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_hunan_attack_2
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_hunan_attack_2
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 602
				state = 1054
			}
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = hunan_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = hunan_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
			set_country_flag = flag_KOR_hunan_attack_3
		}
	}
	KOR_hunan_attack_3 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = hunan_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_hunan_attack_3
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_hunan_attack_3
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 602
				state = 1054
			}
		}
		
		fire_only_once = yes
		days_remove = 10
		complete_effect = { if = { limit = { is_ai =no NOT = { has_country_flag = flag_KOR_hunan_attack_4 } } army_experience = -20 add_command_power = -100 } country_event = { id = korea.352 }
			every_state = {
				limit = {
					has_state_flag = hunan_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
		}	
		remove_effect = {
			set_country_flag = flag_KOR_hunan_attack_4
		}
	}
	KOR_guangdong_attack_1 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = guangdong_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
		}
		visible = { tag = KOR has_war_with = ZLC has_global_flag = global_CHI_defence_the_korea } 
		highlight_states = {
			highlight_state_targets = { 
				state = 1046
				state = 593
				state = 592
				state = 728
				state = 1045
				state = 591
				state = 729
				state = 326
				state = 1098
			}
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = guangdong_province_state
					NOT = {
						state = 729
						state = 326
						state = 728
					}
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = guangdong_province_state
					NOT = {
						state = 729
						state = 326
						state = 728
					}
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
			set_country_flag = flag_KOR_guangdong_attack_2
		}
	}
	KOR_guangdong_attack_2 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = guangdong_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_guangdong_attack_2
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_guangdong_attack_2
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 1046
				state = 593
				state = 592
				state = 728
				state = 1045
				state = 591
				state = 729
				state = 326
				state = 1098
			}
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = guangdong_province_state
					NOT = {
						state = 729
						state = 326
						state = 728
					}
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = guangdong_province_state
					NOT = {
						state = 729
						state = 326
						state = 728
					}
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
			set_country_flag = flag_KOR_guangdong_attack_3
		}
	}
	KOR_guangdong_attack_3 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = guangdong_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_guangdong_attack_3
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_guangdong_attack_3
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 1046
				state = 593
				state = 592
				state = 728
				state = 1045
				state = 591
				state = 729
				state = 326
				state = 1098
			}
		}
		
		fire_only_once = yes
		days_remove = 10
		complete_effect = { if = { limit = { is_ai =no NOT = { has_country_flag = flag_KOR_guangdong_attack_4 } } army_experience = -20 add_command_power = -100 } country_event = { id = korea.352 }
			every_state = {
				limit = {
					has_state_flag = guangdong_province_state
					NOT = {
						state = 729
						state = 326
						state = 728
					}
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
		}	
		remove_effect = {
			set_country_flag = flag_KOR_guangdong_attack_4
		}
	}
	KOR_guangxi_attack_1 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = guangxi_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
		}
		visible = { tag = KOR has_war_with = ZLC has_global_flag = global_CHI_defence_the_korea } 
		highlight_states = {
			highlight_state_targets = { 
				state = 599
				state = 594
			}
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = guangxi_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = guangxi_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
			set_country_flag = flag_KOR_guangxi_attack_2
		}
	}
	KOR_guangxi_attack_2 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = guangxi_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_guangxi_attack_2
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_guangxi_attack_2
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 599
				state = 594
			}
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = guangxi_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = guangxi_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
			set_country_flag = flag_KOR_guangxi_attack_3
		}
	}
	KOR_guangxi_attack_3 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = guangxi_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_guangxi_attack_3
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_guangxi_attack_3
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 599
				state = 594
			}
		}
		
		fire_only_once = yes
		days_remove = 10
		complete_effect = { if = { limit = { is_ai =no NOT = { has_country_flag = flag_KOR_guangxi_attack_4 } } army_experience = -20 add_command_power = -100 } country_event = { id = korea.352 }
			every_state = {
				limit = {
					has_state_flag = guangxi_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
		}	
		remove_effect = {
			set_country_flag = flag_KOR_guangxi_attack_4
		}
	}
	KOR_guizhou_attack_1 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = guizhou_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
		}
		visible = { tag = KOR has_war_with = ZLC has_global_flag = global_CHI_defence_the_korea } 
		highlight_states = {
			highlight_state_targets = { 
				state = 603
				state = 1058
				state = 1053
			}
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = guizhou_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = guizhou_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
			set_country_flag = flag_KOR_guizhou_attack_2
		}
	}
	KOR_guizhou_attack_2 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = guizhou_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_guizhou_attack_2
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_guizhou_attack_2
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 603
				state = 1058
				state = 1053
			}
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = guizhou_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = guizhou_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
			set_country_flag = flag_KOR_guizhou_attack_3
		}
	}
	KOR_guizhou_attack_3 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = guizhou_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_guizhou_attack_3
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_guizhou_attack_3
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 603
				state = 1058
				state = 1053
			}
		}
		
		fire_only_once = yes
		days_remove = 10
		complete_effect = { if = { limit = { is_ai =no NOT = { has_country_flag = flag_KOR_guizhou_attack_4 } } army_experience = -20 add_command_power = -100 } country_event = { id = korea.352 }
			every_state = {
				limit = {
					has_state_flag = guizhou_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
		}	
		remove_effect = {
			set_country_flag = flag_KOR_guizhou_attack_4
		}
	}
	KOR_sichuan_attack_1 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = sichuan_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
		}
		visible = { tag = KOR has_war_with = ZLC has_global_flag = global_CHI_defence_the_korea } 
		highlight_states = {
			highlight_state_targets = { 
				state = 1007
				state = 1047
				state = 753
				state = 748
				state = 605
			} 
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = sichuan_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = sichuan_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
			set_country_flag = flag_KOR_sichuan_attack_2
		}
	}
	KOR_sichuan_attack_2 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = sichuan_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_sichuan_attack_2
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_sichuan_attack_2
		} 
		highlight_states = {
			highlight_state_targets = { state = 605 } 
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = sichuan_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = sichuan_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
			set_country_flag = flag_KOR_sichuan_attack_3
		}
	}
	KOR_sichuan_attack_3 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = sichuan_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_sichuan_attack_3
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_sichuan_attack_3
		} 
		highlight_states = {
			highlight_state_targets = { state = 605 } 
		}
		
		fire_only_once = yes
		days_remove = 10
		complete_effect = { if = { limit = { is_ai =no NOT = { has_country_flag = flag_KOR_sichuan_attack_4 } } army_experience = -20 add_command_power = -100 } country_event = { id = korea.352 }
			every_state = {
				limit = {
					has_state_flag = sichuan_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
		}	
		remove_effect = {
			set_country_flag = flag_KOR_sichuan_attack_4
		}
	}
	KOR_xikang_attack_1 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = xikang_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
		}
		visible = { tag = KOR has_war_with = ZLC has_global_flag = global_CHI_defence_the_korea } 
		highlight_states = {
			highlight_state_targets = { 
				state = 752
				state = 751
				state = 601
			} 
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = xikang_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = xikang_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
			set_country_flag = flag_KOR_xikang_attack_2
		}
	}
	KOR_xikang_attack_2 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = xikang_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_xikang_attack_2
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_xikang_attack_2
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 752
				state = 751
				state = 601
			} 
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = xikang_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = xikang_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
			set_country_flag = flag_KOR_xikang_attack_3
		}
	}
	KOR_xikang_attack_3 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = xikang_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_xikang_attack_3
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_xikang_attack_3
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 752
				state = 751
				state = 601
			} 
		}
		
		fire_only_once = yes
		days_remove = 10
		complete_effect = { if = { limit = { is_ai =no NOT = { has_country_flag = flag_KOR_xikang_attack_4 } } army_experience = -20 add_command_power = -100 } country_event = { id = korea.352 }
			every_state = {
				limit = {
					has_state_flag = xikang_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
		}	
		remove_effect = {
			set_country_flag = flag_KOR_xikang_attack_4
		}
	}
	KOR_yunnan_attack_1 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = yunnan_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
		}
		visible = { tag = KOR has_war_with = ZLC has_global_flag = global_CHI_defence_the_korea } 
		highlight_states = {
			highlight_state_targets = { 
				state = 1010
				state = 747
				state = 325
				state = 1013
			} 
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = yunnan_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = yunnan_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
			set_country_flag = flag_KOR_yunnan_attack_2
		}
	}
	KOR_yunnan_attack_2 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = yunnan_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_yunnan_attack_2
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_yunnan_attack_2
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 1010
				state = 747
				state = 325
				state = 1013
			} 
		}
		
		fire_only_once = yes
		days_remove = 30
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.264 } }
			every_state = {
				limit = {
					has_state_flag = yunnan_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_2 }
			}	
		}	
		remove_effect = {
			every_state = {
				limit = {
					has_state_flag = yunnan_province_state
				}
				add_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
			set_country_flag = flag_KOR_yunnan_attack_3
		}
	}
	KOR_yunnan_attack_3 = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_chi = yes
			has_war_with = ZLC
			any_state = { has_state_flag = yunnan_province_state	OR = { is_controlled_by = KOR any_neighbor_state = { is_controlled_by = KOR } if = { limit = { WZC = { is_puppet_of = KOR } } OR = { is_controlled_by = WZC any_neighbor_state = { is_controlled_by = WZC } } } } }
			has_country_flag = flag_KOR_yunnan_attack_3
		}
		visible = {
			tag = KOR has_war_with = ZLC
			has_country_flag = flag_KOR_yunnan_attack_3
		} 
		highlight_states = {
			highlight_state_targets = { 
				state = 1010
				state = 747
				state = 325
				state = 1013
			}  
		}
		
		fire_only_once = yes
		days_remove = 10
		complete_effect = { if = { limit = { is_ai =no NOT = { has_country_flag = flag_KOR_yunnan_attack_4 } } army_experience = -20 add_command_power = -100 } country_event = { id = korea.352 }
			every_state = {
				limit = {
					has_state_flag = yunnan_province_state
				}
				remove_dynamic_modifier = { modifier = CHI_defence_the_korea_3 }
			}	
		}	
		remove_effect = {	
			set_country_flag = flag_KOR_yunnan_attack_4
		}
	}
}	
KOR_forth_plan_nakil = {
	KOR_chugoku_attack = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_jap = yes
			has_war_with = JAP
			OR = {
				1066 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
				529 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
				989 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
			}
		}
		visible = {
			tag = KOR has_war_with = JAP has_game_rule = { rule = TLE_JAP_HARDCORE_RULE option = TLE_JAP_HARDCORE_DEFAULT }
		} 
		highlight_states = {
			highlight_state_targets = { state = 529 } 
		}
		
		fire_only_once = no
		days_remove = 20
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.266 } }
			529 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			1066 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			989 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
		}	
		remove_effect = {
			529 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			1066 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			989 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
		}
	}
	KOR_sigoku_attack = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_jap = yes
			has_war_with = JAP
			530 = {
				OR = {
					is_controlled_by = KOR
					any_neighbor_state = {
						is_controlled_by = KOR
					}
				}
			}
		}
		visible = {
			tag = KOR has_war_with = JAP has_game_rule = { rule = TLE_JAP_HARDCORE_RULE option = TLE_JAP_HARDCORE_DEFAULT }
		} 
		highlight_states = {
			highlight_state_targets = { state = 530 } 
		}
		
		fire_only_once = no
		days_remove = 20
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.266 } }
			530 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
		}	
		remove_effect = {
			530 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
		}
	}
	KOR_kansai_attack = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_jap = yes
			has_war_with = JAP
			OR = {
				531 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
				988 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
				1070 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
				991 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
				1065 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
			}
		}
		visible = {
			tag = KOR has_war_with = JAP has_game_rule = { rule = TLE_JAP_HARDCORE_RULE option = TLE_JAP_HARDCORE_DEFAULT }
		} 
		highlight_states = {
			highlight_state_targets = { state = 1070 } 
		}
		
		fire_only_once = no
		days_remove = 20
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.266 } }
			1070 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			531 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			988 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			991 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			1065 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
		}	
		remove_effect = {
			1070 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			531 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			988 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			991 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			1065 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
		}
	}
	KOR_chubu_attack = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_jap = yes
			has_war_with = JAP
			OR = {
				1069 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
				535 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
				532 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
				534 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
			}
		}
		visible = {
			tag = KOR has_war_with = JAP has_game_rule = { rule = TLE_JAP_HARDCORE_RULE option = TLE_JAP_HARDCORE_DEFAULT }
		} 
		highlight_states = {
			highlight_state_targets = { state = 535 } 
		}
		
		fire_only_once = no
		days_remove = 20
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.266 } }
			535 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			534 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			532 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			1069 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
		}	
		remove_effect = {
			535 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			534 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			532 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			1069 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
		}
	}
	KOR_kanto_attack = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_jap = yes
			has_war_with = JAP
			OR = {
				990 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
				1067 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
				282 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
				1073 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
			}
		}
		visible = {
			tag = KOR has_war_with = JAP has_game_rule = { rule = TLE_JAP_HARDCORE_RULE option = TLE_JAP_HARDCORE_DEFAULT }
		} 
		highlight_states = {
			highlight_state_targets = { state = 282 } 
		}
		
		fire_only_once = no
		days_remove = 20
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.266 } }
			990 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			282 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			1067 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			1073 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
		}	
		remove_effect = {
			990 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			282 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			1067 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			1073 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
		}
	}
	KOR_tohoku_attack = {
		icon = generic_operation
		allowed = { original_tag = KOR }
		ai_will_do = {
			factor = 10
		}
		available = { if = { limit = { is_ai =no } has_army_experience > 20 command_power > 100 } plan_isnt_started_jap = yes
			has_war_with = JAP
			OR = {
				1072 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
				533 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
				1071 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
				1068 = {
					OR = {
						is_controlled_by = KOR
						any_neighbor_state = {
							is_controlled_by = KOR
						}
					}
				}
			}
		}
		visible = {
			tag = KOR has_war_with = JAP has_game_rule = { rule = TLE_JAP_HARDCORE_RULE option = TLE_JAP_HARDCORE_DEFAULT }
		} 
		highlight_states = {
			highlight_state_targets = { state = 533 } 
		}
		
		fire_only_once = no
		days_remove = 20
		complete_effect = { if = { limit = { is_ai =no } army_experience = -20 add_command_power = -100 country_event = { id = korea.266 } }
			1072 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			533 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			1071 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			1068 = {
				remove_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
		}	
		remove_effect = {
			1072 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			533 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			1071 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
			1068 = {
				add_dynamic_modifier = { modifier = JAP_defence_the_korea }
			}
		}
	}
	
}