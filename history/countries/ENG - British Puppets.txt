recruit_character = ENG_queen_elizabeth_ii

puppet = EGY
puppet = SRL
add_to_faction = IRE

IRE = {
	set_cosmetic_tag = IRE_UK
}

if = {
	limit = {
		OR = {
			has_dlc = "Together for Victory"
			has_dlc = "Man the Guns"
		}
	}
	set_autonomy = {
		target = SAR
		autonomous_state = autonomy_dominion
		freedom_level = 0.2
	}
	set_autonomy = {
		target = BOR
		autonomous_state = autonomy_colony
		freedom_level = 0.2
	}
	set_autonomy = {
		target = SST
		autonomous_state = autonomy_colony
		freedom_level = 0.2
	}
	else = {
		puppet = SAR
		puppet = SST
		puppet = BOR
	}
}